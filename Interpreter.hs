module Interpreter where
import Expressions


{- Do I need to make a new parser for these expressions?
   Yeah, it appears to be that way.
   Oh, but that's not too bad, it appears everything else is
   hella LL(1). -}
